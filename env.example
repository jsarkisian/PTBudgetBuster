# MCP-PT Configuration
# Copy this to .env and fill in your values

# Required: Your Anthropic API key for AI-assisted testing
ANTHROPIC_API_KEY=sk-ant-xxxxx

# JWT secret for session tokens (change this!)
JWT_SECRET=your-secure-random-string-here

# Server ports
BACKEND_PORT=8000
FRONTEND_PORT=3000

# CORS - add your server's domain/IP
ALLOWED_ORIGINS=http://localhost:3000,https://your-server.com

# Optional: Tailscale integration
# To bind MCP-PT to your Tailscale IP only (not publicly accessible):
# FRONTEND_PORT=100.x.y.z:3000
# BACKEND_PORT=100.x.y.z:8000
# ALLOWED_ORIGINS=http://100.x.y.z:3000

# Optional: Tailscale exit node
# Route all outbound traffic through this exit node on deploy.
# Use the node's hostname or IP from 'tailscale status'.
# TAILSCALE_EXIT_NODE=my-exit-node

# Optional: subfinder API keys for better reconnaissance
# Add to configs/provider-config.yaml
